---
import { IMAGES_VERCEL_STORAGE } from '@/constants/images-vercel-storage'
interface Props {
  alt: string
  captureName: string
}

const { captureName, alt } = Astro.props
---

<img src=`${IMAGES_VERCEL_STORAGE}/${captureName}` alt={alt} />
